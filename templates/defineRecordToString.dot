{{= it.toStringFn }} : {{= it.type }} -> List (String, String)
{{= it.toStringFn }} val =
    Dict.empty
        {{~ it.members :m }}
        |> Dict.insert
            "{{= m.key }}" "" -- val.{{= m.key }}
        {{~}}
        |> Dict.toList
